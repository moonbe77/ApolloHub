import {colors} from './index'

export const theme = {
  light: {
    backgroundColor: '#fff',
    color: '#000',
    textColor: 'green',
  },
  dark: {
    backgroundColor: '#222',
    color: '#fff',
    textColor: 'gray',
  },  
  colors:{
    ...colors,
  }
}
